---
export interface Props {
   themeClass: string;
}

const { themeClass } = Astro.props;

const themeName = themeClass.replace('theme-', '');
---

<li class={`${themeClass} bg-base-200 text-base-content`}>
   <h2 class="text-base-content-emphasis">{themeName} theme</h2>

   <p>
      Lorem ipsum dolor sit amet elit asperiores dolores optio, amet adipisci
      modi perferendis voluptas molestiae rem tempore fuga asperiores voluptate
      ab dolor vel! Lorem ipsum dolor sit amet consectetur adipisicing elit.
   </p>

   <button
      class="bg-primary text-primary-content"
      onclick={`document.documentElement.className='${themeClass}'`}
   >
      Select
   </button>
</li>

<style>
   li {
      @apply rounded-3xl p-7 flex flex-col gap-1 items-start shadow-lg;
   }

   h2 {
      @apply font-extrabold capitalize text-xl;
   }

   p {
      @apply line-clamp-2;
   }

   button {
      @apply mt-2 px-5 py-2 rounded-full text-sm;
   }
</style>
